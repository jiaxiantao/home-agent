---
description: 每次对话有代码改动时只本地提交；用户明确要求时才推送到 master
alwaysApply: true
---

# Git 提交与推送约定

默认分支：`master`（不要再使用 `main`）

远程仓库：

- `origin` → `git@github.com:jiaxiantao/home-agent.git`
- `souche` → `git@git.souche-inc.com:dfc-ai/dfc-data-agent.git`

当本次对话中对仓库产生了代码或配置改动时，在回复用户前完成：

1. 并行执行 `git status`、`git diff`、`git log -1 --oneline`。
2. 不要提交 `.env` 或含密钥的文件；`.env.example` 可以提交。
3. 有改动时：`git add`（相关文件）→ `git commit`（HEREDOC 写清「为什么」）。**不要自动 push。**
4. 只有用户明确说「推送 / push / 推到远程」时，才执行 `git push origin master` 与 `git push souche master`。
5. 若无任何可提交改动，不要空提交。
6. 不要使用 `git push --force` 到 `master`，除非用户明确要求。

在最终回复中简短告知：commit hash（如有）、分支；未要求推送则注明「未推送」。
