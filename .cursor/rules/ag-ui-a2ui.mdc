---
description: 统一遵循 AG-UI 传输协议与 A2UI 生成式 UI 规范
alwaysApply: true
---

# AG-UI + A2UI 约定

本项目的 Agent 前后端交互默认遵循两层协议：

- `AG-UI`：负责 Agent 与用户界面的双向事件通信、状态同步与交互回传。
- `A2UI`：负责 Agent 生成的声明式 UI 结构，使用可校验的 JSON 消息描述 surface、component、data model 和 action。

实现或重构相关能力时，优先遵守这些规则：

1. 不要把最终 UI 片段作为自由文本、HTML 字符串或不可校验的 Markdown 片段塞进响应里；优先输出可验证的结构化对象。
2. 传输层与渲染层分离：`AG-UI` 负责事件流、会话状态、用户动作回传；`A2UI` 负责界面描述，不把两者耦合在同一个临时数据结构里。
3. 事件命名、消息类型和 payload 应保持稳定、可版本化；新增字段优先向后兼容，避免破坏已有客户端。
4. 前端渲染器必须把服务端返回内容当作数据协议处理，而不是执行任意代码。
5. 需要交互式卡片、表单、列表、操作按钮时，优先设计为 `A2UI` surface / component / action，而不是继续扩展仅面向日志展示的纯文本 trace。
6. 若现有实现暂时仍使用 SSE 或自定义事件，新增设计应显式考虑如何映射到 `AG-UI` 事件模型，并为迁移到标准协议保留边界。

简单判断：

- “怎么传” 问题，按 `AG-UI` 设计。
- “渲染什么” 问题，按 `A2UI` 设计。
- “用户点击后怎么回传给 Agent” 问题，按 `AG-UI` action / event 往返设计。
